environments:
  local:

---

helmDefaults:
  verify: false
  wait: true
  timeout: 300
  recreatePods: false
  force: false
  createNamespace: false

helmfiles:
  - ./helmfile-jaeger.yaml
  - ../services/user/deploy/helmfile.yaml
  - ../services/gateway/deploy/helmfile.yaml