<source>
  @type kafka_group
  brokers kafka:9092
  topics logs
  consumer_group fluentd_group
  # Use 'none' format if your logs are plain text; adjust if you use JSON or another format.
  # TODO
  format none
</source>

<match **>
  @type loki
  endpoint_url "http://loki:3100/loki/api/v1/push"
  tenant_id "default"
  <buffer>
    flush_interval 1s
  </buffer>
  <label>
    job logs
  </label>
  <label>
    hostname ${hostname}
  </label>
</match>